{
  "formatVersion": 1,
  "database": {
    "version": 1,
    "identityHash": "keyboard-schema-v1",
    "entities": [
      {
        "tableName": "words",
        "createSql": "CREATE TABLE IF NOT EXISTS `words` (`word` TEXT NOT NULL, `language` TEXT NOT NULL, `frequency` INTEGER NOT NULL, `nextWordHint` TEXT, PRIMARY KEY(`word`))"
      }
    ]
  }
}
